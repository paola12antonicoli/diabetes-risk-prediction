__pycache__/
.ipynb_checkpoints/
*.pyc
.env
.DS_Store
models/*.pkl
models/*.joblib
# Note: model artifacts are excluded because the Random Forest serialised file
# exceeds GitHub's 100MB limit. Re-run the notebooks to regenerate them.
